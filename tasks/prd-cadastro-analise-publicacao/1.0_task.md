# 1.0 - Mapeamento de Código e Preparação

## Objetivo
- Localizar com precisão os pontos de alteração no frontend e backend para garantir que o desacoplamento não afete outras partes do sistema.

## Escopo / Entregáveis
- Lista de arquivos e números de linha onde ocorrem:
    - O cadastro de prazos no fluxo de similaridade.
    - A renderização dos botões no card de similaridade.
    - A chamada ao endpoint de confirmação de análise.

## Passos e subtarefas
- 1.1 Identificar a função no frontend que dispara o "Cadastrar" (provavelmente em `public/js/similaridade.js`).
- 1.2 Localizar o método no backend que processa essa requisição (provavelmente em `src/controllers/similaridadeController.js`).
- 1.3 Mapear o uso do `prazoRepository.create` no fluxo de cadastro atual.
- 1.4 Identificar o controller do endpoint `/api/analise/confirmar`.

## Dependências
- Nenhuma.

## Paralelizável?
- Sim, pode ser feita em paralelo com a tarefa 2.0.

## Critérios de aceite
- Todos os arquivos e funções necessários identificados e documentados.

## Testes
- N/A (Tarefa de análise).

## Notas
- Focar especialmente em garantir que o `tenant_id` esteja disponível em todos os novos pontos de persistência.

# 2.3_task - Atualizar conciliacaoService e dashboardService

## Objetivo
- Ajustar os fluxos de conciliação e as consultas do dashboard para a nova estrutura de prazos.

## Escopo / Entregáveis
- Alterações em `src/services/conciliacaoService.js` e `src/services/dashboardService.js`.

## Passos e subtarefas
- [ ] 2.3.1: Em `conciliacaoService.js` (`confirmarAnaliseSimilaridade`): Ajustar a criação do prazo para usar o `processoId` resolvido no fluxo, em vez de depender apenas do `publicacaoId`.
- [ ] 2.3.2: Em `dashboardService.js` (`getPrazosDetalhes`): Simplificar a query para buscar prazos diretamente pelo `processoid`. Remover joins desnecessários com a tabela de publicações para obter o número do processo.
- [ ] 2.3.3: Incluir o `status_id` e informações de status nas respostas das APIs.

## Dependências
- [x] Task 1.2 (Estrutura de banco pronta).

## Paralelizável?
- Sim, com a Task 2.1.

## Critérios de aceite
- Prazos gerados via conciliação vinculados corretamente ao processo.
- Dashboard carregando prazos de forma mais performática e exibindo status.

## Testes
- Simular uma conciliação e verificar o registro criado na tabela `Prazo`.
- Validar o payload de retorno do endpoint de detalhes do dashboard.

## Notas
- A simplificação da query no dashboard deve resultar em ganho de performance perceptível.
